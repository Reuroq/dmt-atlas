You have been moved to a Linux server to keep working; the owner's PC is off. Same repository, same files, same job. Differences: the shell is bash (no PowerShell), the repo is at /home/clawd/dmt-atlas (you are in it), Chrome for headless checks is Playwright's chromium (already installed; PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright), node is present, there is no pixelvision screen.cmd here so inspect renders with view_image directly (view each image once), and there is no GPU: renders come from SwiftShader so judge form, colour and motion, not frame rate. Fix any Windows-only paths in your scripts (backslashes, PowerShell calls) as you meet them. Now read world/NOTES.md (your own notes), then world/REDIRECT4.md, world/REDIRECT3.md and world/REDIRECT2.md, and continue with your own plan: every stage and being to RECOGNISE, the coverage test on every stage, acceptance intact. Keep status.txt and latest.png current; rewrite NOTES.md at the end of the turn. Put <<WORLD_DONE>> alone on the last line of NOTES.md only when it all passes.
