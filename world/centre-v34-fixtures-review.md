# v34 fixtures complete — numerical execution not started

No v34 field samples, browser, actual GLSL, cost, capture, image inspection or
acceptance ran. Candidate/oracle/original renderer and live/source/manual
evidence are unchanged. latest.png remains inspected REJECTED GAME v33r6 entry.

## GPU and independent roots

The leaf fixture reads five smooth base jets and eleven signed/offset CSG jets,
plus selected apertures/composite and cheap jets. Aperture maxima are never
treated as smooth. Float32 replay follows nested GLSL ties: right branch for
equal jmax, earlier branch for equal union. Composite FD disagreements retain
selected leaf IDs/one-sided gradients, not a claim of unique crease normals.

Original gates remain: gradient<.005, scalar<.001, directional ratio<=1.001,
root difference<.03, zero misses/missing references, exact CSG/normal replay and
count invariants. Added expanded-jet/aperture wiring checks require equality.
48cases/75996rays/720fixed references retain both grids/modes/cameras and prior
times2.4077/3.5242/4/4.895/8; odd grid also221/900. Both720-reference audits
retain original six plus central3x3 rays. No limit was relaxed.

Interval audit uses csg_interval_v34.enclosure: expand signed leaves BEFORE
endpoint extrema/Taylor error; map five declared Hessians to eleven leaves.
Non-envelope Taylor domain is checked. Earliest-interval left-first refinement,
<=1e-7 true crossing brackets, .03 comparison and fail-on-unresolved/tangency
remain. Ordinary float64 plus pad, not directed-rounding proof.

## Matched cost scope

The reference changes exactly one full-trace call to fieldPartsReach(p,...),
recomputing commonParts instead of consuming retained jets. Geometry, materials,
reach/march expressions, cheap certificate and frame pump are identical v34.
Both arms use identical four-readback instrumentation/timing.12pairs retain
balanced6/6 order, no warmup,49x33,both modes/cameras,times2.4077/3.5242/4.895.
Exact hits/counts, lower estimated prefix work and total/median wall<=1 at BOTH
boundaries remain gates. Exact points/gradients,24completed arms and clean
lifecycle are additionally required. Prefix estimates are not GPU instructions.
This tests sharing on NEW v34, NOT v34-v33r6 speedup or full-resolution speed.
Existing lifecycle contracts are inherited via exact version-only source
equivalence; the old experiment was not replayed.

## Static checks and preserved failure

Builder ONCE actual exit0. Initial checker ONCE exit1: global call-count
assertion also counted an unchanged wrapper. No shader/browser/field execution
was reached. Original checker/log/exit preserved. Revised checker ONCE exit0
checks unique OLD call and exact single-edit construction, also after shader
instrumentation. Candidate/generated fixtures were not changed.

AST/JS syntax, four fake-link fail-closed guards,22leaf outputs, schedule counts,
1012synthetic CSG/tie/sign selections and new runtime failure/replay/foreground
exit contracts PASS. No actual GLSL compilation. Prior interval contracts
inherited, not new field evidence. Launcher-only amendment adds prerequisite
actual exit0/log+launch hashes. Four NEW amendment checks PASS: missing exit,
nonzero exit, changed log, valid success. Unchanged full checker not replayed;
centre-v34-launcher-exit-check.json binds final launcher source.

## Next phase

Full centre-v34-fixture-integrity.json authorizes numerical gates, NOT capture.
Execute foreground ONCE, each only after prior PASS/actual exit0:

1. `python3 world/run_centre_v34_gate_once.py bounds`
2. `python3 world/run_centre_v34_gate_once.py gpu`
3. `python3 world/run_centre_v34_gate_once.py roots`
4. `python3 world/run_centre_v34_gate_once.py cost`

Launcher sets dependencies and saves unique launch/log/actual-child-exit files.
GPU saves completed cases; cost saves each arm outside timed boundaries.
Hardkill/storage loss can still leave incomplete cause-unknown attempts; never
replay or infer an exit. Only allPASS permits NEW centre-v34-integrity.json and
then original1200x800 HIGH/90s renderer ONCE. No numerical closure script yet.
All19/source/coverage/route/full acceptance unfinished; no promotion/WORLD_DONE.
