# Distinct v34 transport-only probe

Scope: Python/Playwright number serialization, JavaScript ingress and Float32Array construction. No Three.js, WebGL, shaders, field samples, images or acceptance. Frozen r2 and its fatal-input/fatal-disposal results remain immutable. Exact uint32 equality is mandatory, including signed zero.

Offline preparation calls the installed Playwright serializer on the original 1024-component texture and a mixed signed-zero/finite binary32 fixture. Save the wire object before classification. Compare decoded wire bits with source bits, not numeric equality. Test integer-bit and JSON-text alternatives. Node checks reconstruct bits independently and exercise partial-resource accounting with fake disposables only. This is not browser or GPU evidence.

The prepared foreground runtime is a NEW one-shot transport experiment, never an arithmetic replay. For each fixture, record input intent, Python wire, browser ingress bits, CPU Float32Array bits and JSON stringify/parse roundtrip bits. Arms: float list (diagnostic failure expected), uint32 list, JSON text, and JS-local signed-zero control. No monkeypatch of Playwright or historical probes. Integer-bit transport must preserve every bit. JSON stringify is expected to erase negative-zero signs; that lossy path is diagnostic only.

Resource accounting uses identities registered immediately after acquisition. Cleanup attempts each registered resource exactly once, continues after an exception, and attempts renderer disposal separately. Require registered/attempted/succeeded identity equality, no duplicate registrations/attempts and renderer success; report errors without erasing an earlier primary failure. Check zero, partial three and full eight resources, a resource exception and renderer exception. Fake-resource results establish accounting behavior only, not actual GPU disposal or leak absence. r2's three reported disposals match its three registered resources before the input stop; retain its fixed-eight fatal classification.

One-shot exclusive launch/log/actual-exit receipts and event hashes preserve failures. Any partial attempt blocks replay. Save complete results before checking. Browser messages and close failures are failures; no arithmetic support can be awarded. Future preparation of an arithmetic successor requires closure of this distinct runtime and must retain all numerical/root/visual gates.
