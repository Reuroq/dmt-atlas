# Canonical-JSON successor — isolated v35r2 field, not a new visual candidate

The previous global attempt remains exit1 / false / ineligible. Its terminal
manifest and actual-exit0 close receipt are verified before successor preparation.
No old worker or launcher is replayed. No saved failed global roots are certified.

## Explicit eligibility

The graph is **new global → new combination**, in `centre-v35r3-canonical-*`.
Both require the immutable v35r2 bounds/numerical/local successful actual exits,
full inventories, sources and prerequisites, and the failed terminal history.
Original numerical exit0 means non-reference readiness, not legacy scan PASS.
Global computes fresh from depth zero using only the accepted numerical inputs;
local records are eligibility evidence only and never algorithm hints. Combination
requires a successful new global receipt; it cannot select the failed old result.
Static closure and its successful actual exit must precede either new attempt.
Any failed/partial/new late exit blocks combination permanently; exclusive claims
prevent replay. Cost/capture remain unprepared and blocked even after combination.

## Only assessment change

`finish_global` durably saves `raw.json` (exclusive link, fsync file/directory),
then reads that exact canonical JSON back before joins and strict assessment.
No scalar coercion is inserted into predicates. `finite`, `global_valid`, the
entire global worker algorithm, transport, scans, local oracle and combination
contracts remain unchanged. New imports and eligibility wiring are explicit.
The new global launch does not consume any historical global raw/result records.

## Static closure

Freeze/check/close have independent exclusive foreground actual-exit receipts.
Source reversal and full AST equality constrain the numerical algorithm and
predicates. A real NumPy float64 → durable JSON → production `finish_global`
regression exercises all 720 synthetic identities; assessed records must have
native scalar types. Native/NumPy equal-value acceptance differs before saving;
strict negative cases still fail after saving. Failed writes/reads cannot produce
certification, and readbacks occur after completed durable publication.
Real foreground synthetic workers test success, false, exception, partial, signal,
late exit, tampering and replay. Eligibility tests exercise the real graph under
explicit synthetic receipt overrides. No field module/browser is used in fixtures.

Static success grants no numerical, visual, coverage, route or acceptance credit.
All19 remain unfinished; latest remains the inspected REJECTED GAME v33r6 image.
