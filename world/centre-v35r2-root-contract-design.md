# Additive strict root certificates — frozen design/core, NOT runtime fixtures

## Evidence and scope

The v35r2 preparation is static PASS, not an actual-field fix. Its 18 derived
sources and all v34 failures remain immutable. Existing four-ray CPU profiles
have entering/exiting pairs inside single .003 grid cells, with outer width
bounds .000883640, .000587943, .000864969 and .000864969. This supports a local
scan-resolution explanation; it does not prove earliest roots or GPU equivalence.

This subphase freezes an oracle-free evidence-combination contract and source
anchors only. It does NOT prepare a runnable replacement for bounds, GPU,
independent roots, cost or capture. No original numerical launcher is authorized
by this contract. The implementation/integration subphase must close separately.

## New, explicitly split evidence graph

1. A distinct future numerical runner must preserve the entire original
   48-case / 75,996-ray schedule, raw GPU outputs, .003 forward scan, 24 scan
   bisections, every found/missing reference and original fixed-grid verdict.
   Save those records BEFORE additional root work. Never overwrite old receipts
   or run an old probe to obtain inputs for this extension.
2. Compute `non_reference_checks_passed` from every conjunct in the frozen
   original case predicate except its final fixed-reference conjunct; check
   all 48 cases, ray totals, no missing GPU hit, browser errors or NaNs. Retain
   all scalar/gradient/bound/count/CSG/expanded/aperture/normal conditions.
   Source audit found that the existing cheap/full predicate is `<.00001`,
   despite shorthand notes saying exact. Preserve that original verdict AND
   require a separate exact binary32 cheap/full equality check in the successor.
   No tolerance is widened; a non-root failure blocks all downstream work.
3. Use an independently enumerated list of exactly 720 unique reference keys
   (width,height,z,time,high,ray), not `zip` truncation or keys selected from hits.
   Require bijections between expected keys and every old/local/global record.
   Historical four-ray identities are not an allowlist for new failures.
4. For EVERY new scheduled reference ray, take a distinct float64 CPU local
   profile over saved GPU depth +/- .004, 161 depths at .00005 spacing. Record
   full scalar and all 11 expanded leaves, points, active CSG branch and source
   hashes; retain actual GPU point separately from reconstructed CPU-ray points.
   The extent is a frozen diagnostic domain, not a new root-error tolerance.
5. Enumerate every local strict full-field entering AND exiting bracket. Retain
   exact zeros separately; a sampled exact zero makes this contract unresolved,
   never permission to use a later crossing. Refine each bracket 24 times with
   complete endpoint/midpoint full/leaf records; no nearest-root selection.
   The oracle-free core validates the grid, all bracket identities, strict
   signs and all 24 bisection transitions. Full/leaf oracle provenance and replay
   remain required responsibilities of the future runner and saved-only closure.
6. Only after all non-root and local evidence passes may a DISTINCT independent
   root worker run. Its `first` recursion, global coarse interval construction,
   radial-domain/envelope exclusions, eleven-leaf Taylor enclosure, left-first
   traversal, depth32 budget, strict positive/negative bracket <=1e-7,
   unresolved failure and .03 depth comparison must be unchanged. Do not give
   that algorithm local hints, narrower search domains or a GPU-nearest target.
   It must search all 720 rays from depth0. Preserve its full independent result.
   Entry eligibility is a NEW `root_certification_ready` receipt, not a forged
   `baseline.passed`; actual exit0 means readiness only, NEVER numerical PASS.
7. The independent first bracket must lie wholly inside exactly one ORIGINAL
   local positive/negative sample cell. Check the original cell, not the much
   narrower local refined bracket; the latter remains retained separately.
   Ambiguous, disjoint or cell-boundary-straddling correspondence FAILS. This
   correspondence does not assert unique roots within the cell; earliest-root
   evidence comes only from the independent global algorithm.
8. Combine exactly 720 complete records. All old FOUND references must still
   meet their original strict difference<.03 requirement. MISSING references
   remain missing in the old result, and need both local and independent
   evidence like every other ray. Recompute independent width/sign/difference
   and reject unresolved output regardless of its claimed pass flag. Publish
   `original_fixed_grid_passed` separately from `additive_reference_contract_passed`.
   Acceptance cannot consume this contract alone: require the separate trusted
   non-root/exact-jet gate, independent algorithm/source/lifecycle/exit receipts,
   all720 record coverage and then the unchanged matched-cost and capture gates.

## Static checks in this subphase

Bind the original full case predicate, its non-root conjuncts, schedule and
independent recursion to AST hashes; no numerical module import or field call.
Test the contract on a synthetic narrow entering/exiting pair missed by the
coarse endpoints, then adversarial mutations: absent/duplicate/extra identities,
bad signs or widths, zeros, missed exit crossings, changed bisection history,
wrong global ordering, unresolved intervals, dishonest flags, bad legacy depth,
NaN/infinity/type confusions and a missing independent certificate. Preserve
frozen source/static/output/actual-exit receipts. Static PASS is not a root proof.

## Held limits and next integration

No candidate edits. Preserve x+-4, entry8/exit-22, closure beyond-27.5,
clearance>=4, HIGH2048/LOW1536, 14 marcher bisections, .00015 residual/directional
test, no minimum step, centre-3.2..0,45s exit,1200x800 HIGH/90s waits and pixel
equality. Keep live/defaults/source/coverage/realism ledgers untouched.

Next prepare DISTINCT versioned numerical/local/global/combination runners,
fixtures and launchers implementing this graph. Prove exact reversibility of
all added instrumentation, unchanged old scan and independent algorithm AST,
strict non-root predicate transfer, bijective joins, durable data BEFORE
assessment, Uint32 transport where exact words matter, and foreground failure/
partial/replay/lifecycle contracts. Freeze and statically close THAT full suite
before any field runtime. Existing v35r2 launchers remain unexecuted; cost,
capture and all19 realism/source/coverage/route/full acceptance remain blocked.
