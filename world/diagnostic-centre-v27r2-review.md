# Isolated v27r2 full-resolution visual attempt — rejected for promotion

Inspected `diagnostic-centre-v27r2-entry.png` exactly once with `view_image`
after `render_centre_v27r2.py` exited. No historical images were re-viewed.
The image is a synthesised candidate diagnostic, not a witness image or live
journey acceptance capture. The HTML visibly labels the isolated build.

## Observation: GAME (single-frame diagnostic judgement)

At 1200×800 HIGH, entry [0,1.7,8], animation time 3.8097999996, the centre
has a blue/cyan clover-like folded shape, green/yellow rounded lobes and a
red/pink surround. It is no longer visually just an angular fan converging
on a point. No obvious axial hue discontinuity is visible in this frame.
However, the central construction is still small, rounded and simple rather
than overwhelming nested geometry. Large outer magenta/purple surfaces remain
smooth and almost empty. Intermediate green/cyan/violet layers show many fine
streaks, serrated-looking edges and smeared gradients, not consistently resolved
jewelled detail. Saturation and local highlights are strong; coherent glass,
chrome and deep multilayer material structure are not convincing. No substantial
black openings are visible. This does not meet RECOGNISE.

The fine edge marks are an unresolved sampling/shading concern, NOT identified
GPU ray misses. One still cannot establish their cause or temporal stability.
The candidate's sampled numerical pass does not settle this visual issue.

## Failed temporal run and limits

The first capture passed HIGH, paused, no transition, settled render, matching
animation clocks, zero missing evidence/uncited meshes, zero console/page errors
and zero veil opacity. The normal Begin/Next controls reached the scene. No
pose, route or clock was injected. The renderer then unpaused normally, waited
for at least three further animation seconds, and paused normally. The second
capture failed its unchanged 90,000ms settled-render wait. No second screenshot
or deep capture was produced. No failure-time diagnostics were saved, so the
exact pending-state/clock mismatch cannot be recovered from this attempt.

The launch/navigation-to-pause measurement was 4.788s; it excludes entry GPU
settling and must not be presented as render time. No completed temporal FPS
measurement exists. The failed settling wait is adverse full-resolution
SwiftShader runtime evidence, not a hardware-GPU benchmark or proof of the
original live physical-exit failure's cause. No timeout extension or retry was
performed. Temporal aliasing, breathing/folding, deep centre quality and complete
runtime performance remain unreviewed. The full four-frame diagnostic did NOT
pass. Preserve the script, HTML, log, receipt, partial summary and PNG unchanged.

## Decision and next work

Do not promote v27r2. Its single-frame GAME judgement already requires another
substantive revision; completing more expensive captures of this unchanged
candidate is not an acceptance pass. First address candidate full-resolution
cost and the unresolved serrated/smeared detail, retaining the validated field,
gradient, first-root and clearance requirements wherever relevant. Then obtain
NEW full-resolution entry/deep temporal evidence. Do not loosen numeric gates,
silently lower capture resolution, or extend the original 45-second exit test.

Live continuum v25/trip v16, source decisions, realism/coverage ledgers and
default live capture remain unchanged. `latest.png` now shows this inspected,
explicitly labelled diagnostic, not a promoted runtime. Live GAME, coverage
84.7% FAIL (top-ten dark ABSENT), source decisions 50/20 and the original
45-second exit failure remain. Other 18 realism targets, 16 source audits,
route/timing review and full acceptance remain unfinished.
