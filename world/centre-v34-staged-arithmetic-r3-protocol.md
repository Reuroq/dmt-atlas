# Distinct staged arithmetic r3 — static preparation first

Never modify or replay frozen r2. This revision follows the verified transport runtime closure.
It is synthetic arithmetic evidence only: no journey field, image, realism, root, cost or acceptance claim.

## Preserved experiment

Original frozen seeds and 128×2 RGBA32F texture; original vertex/fragment arithmetic;
literal versus second-offset-only uniform arms for both childAxial and childAngular;
126 ordered GL-localized stages with known-float control first; same full Three/raw
512-component readbacks saved durably before assessment and separately staged shader queries.
No translated shader queries. Exact rational ties-even oracle covers 1,024 components.
Unchanged support criterion requires both leaves and both read methods: literal child-versus-parent
mismatches with all other channels exact, and exact uniform unique samples. Infrastructure must be clean.
Support remains synthetic and cannot erase original exact-jet/716-of-720 field failures.

## Narrow corrections

- Python sends integer source uint32 words, never a float list; JS reconstructs Float32Array over
  Uint32Array storage. Source/request/CPU words are strictly typed and bit-exact including zero signs.
- Immediately register every successfully constructed renderer, target, texture, geometry and material.
  Mesh receives null material to avoid an implicit untracked material before explicit shader assignment.
  Snapshots retain stable object identities, kinds, attempt/success/error state. Registry loss is fatal.
- Cleanup attempts all registered resources once, renderer last, continuing after any disposal exception.
  It still runs when the GL error queue is dirty, undrained or throws. Dirty GL state remains fatal;
  attempted cleanup is not automatically a clean runtime. Repeated cleanup returns retained evidence,
  without retrying disposal. Compare identities, never a fixed count. Preserve primary and cleanup failures.
- Unchanged historical missing-API reproduction is not replayed. New mocks exercise actual successor
  HTML under normal/partial allocations, full nine-identity allocation, malformed and signed-zero ingress,
  constructor/setter failures, duplicate registration, GL drain failures and every disposer throwing.
  Mocks prove JS control/accounting behavior only, not GPU cleanup/leaks or arithmetic.

## Exclusive sequence

1. Create distinct sources and a reversible source-delta record; freeze after verifying prior closure.
2. Run `python3 -B world/run_centre_v34_staged_arithmetic_r3_once.py static` ONCE foreground.
3. Inspect its saved receipt, then run the same launcher with `close` ONCE; saved-only pass/failure closure.
   Update NOTES (BOM retained), README/status once, archive prior display. Preserve latest.png unchanged.
4. Only a passing closed static manifest and actual-exit0 static/close receipts permit a later bounded
   `runtime` ONCE foreground. Wait for actual exit; close saved full results and lifecycle before inference.

Exclusive fsynced Node source/command/stdout/stderr/exit receipts precede classification.
The outer launcher records actual subprocess exit, never treating a launcher error as probe success.
No automatic retries, candidate promotion, relaxed bit tests or new images in static preparation.
