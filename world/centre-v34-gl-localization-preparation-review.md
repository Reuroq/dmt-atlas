# GL localization preparation CLOSED — runtime NOT RUN

Current follow-up runtime integrity verified before work: 407 evidence, 16
protected and four display hashes match; runtime closure actual exit0 and bound
launch/log/script hashes match. No superseded runtime_gate() was used.

Inspected the carried failed HTML and bounded bundled-Three source excerpts.
The old final error check follows setup/render, program/shader/debug queries and
readback. Three calls deleteShader after linking; retained-handle queries are
therefore worth isolating, but their validity and the actual GL1282 source remain
unestablished. Successful old compilation/linking is not localization evidence.

New frozen files implement four infrastructure-only controls and 117 ordered
stages. Every operation has a durable pre-intent and result; the result includes
consumed pre/post errors, exceptions and context loss. Framebuffer state and
exact 512-component readbacks precede shader/debug queries. Full arrays/bits
survive validation failures. Query errors stay recorded and force nonzero exit,
even if the diagnostic completes. Core failures stop subsequent controls.

Static checker ran ONCE, actual exit0, with bound launch/log/exit receipts. Checks:
Python AST and JS syntax; generated plan/independent known-texel values; mocked
GL errors, dirty preconditions, exception-finally capture, drain bound and context
loss; nonfinite/signed-zero packing; one-bit/sentinel/NaN readback rejection;
fatal/query policy and framebuffer/link/callback rejection; and mocked child
exit7 receipt/replay refusal under a disposable world/ directory. That synthetic
exit7 is not a browser run. Static mocks do not establish real GL behavior.

Scope limits: Three method stages bound intervals containing internal calls;
they cannot name an internal failing GL operation. A renderer-construction
exception may preclude access to the context. Abrupt termination leaves an
unanswered durable intent. Missing optional extension is recorded as skipped.
Empty translated strings are not evidence of a compiler cause.

Next phase: verify preparation hashes, then run only
`python3 -B world/run_centre_v34_gl_localization_once.py` once in foreground.
Wait for its actual exit before inspecting results; retain failures and do not
edit/retry frozen code. Review ordered errors/framebuffers/readbacks/query
lifetime evidence before choosing further work. No arithmetic conclusion or
v35 candidate follows from these controls, even if all are clean.

No browser, GLSL, oracle, old scan, independent-root, cost, capture or acceptance
runtime; no new image/inspection/promotion. latest remains the inspected REJECTED
GAME v33r6 entry. Live/defaults/ledgers unchanged; original numerical FAIL and
all19/source/coverage/route/full acceptance remain unfinished.
